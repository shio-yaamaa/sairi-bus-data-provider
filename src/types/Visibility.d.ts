export interface VisibilityData {
  classwork: boolean;
  bus: boolean[]; // An index corresponds to a sectionIndex
  library: boolean;
  restaurant: boolean[]; // An index corresponds to a campusIndex
}